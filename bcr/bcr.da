
class BCR(process):
	









def main():
    nclients = int(sys.argv[1]) if len(sys.argv) > 1 else 3
    nReplicas = int(sys.argv[2]) if len(sys.argv) > 1 else 3
    config(clock='Lamport')
    pong = new(Olympus,nReplicas, num= 1)
    ping = new(Ping, num= npings)
    setup(ping, (pong, nrounds))
    start(pong)
    start(ping)
	